language: java
jdk:
  - openjdk8

branches: #어떤 브랜치
  only:
    - init

# Travis CI 서버
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build" # 3.

# CI 실행 완료 시 메일 알람
notifications: # 4.
  email:
    recipients:
      - nmj5012@naver.com